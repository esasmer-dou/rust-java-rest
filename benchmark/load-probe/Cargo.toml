[package]
name = "load-probe"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
reqwest = { version = "0.12", default-features = false }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
